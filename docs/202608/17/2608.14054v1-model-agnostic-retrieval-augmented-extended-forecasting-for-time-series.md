---
title: Model-agnostic Retrieval-Augmented Extended Forecasting for time series
title_zh: 模型无关的检索增强扩展预测用于时间序列
authors: "Juan Pablo Villa Serna, Rohan Asthana, Vasileios Belagiannis"
date: 2026-08-14
pdf: "https://arxiv.org/pdf/2608.14054v1"
tags: ["query:time-series"]
score: 9.0
evidence: 基于预训练基础模型与检索增强的时间序列预测
tldr: 时间序列预训练模型在零样本场景表现好，但在历史数据稀缺的领域应用中，微调虽有效却计算开销大。为此，基于检索增强预测(RAF)，本文提出模型无关的RAEF：通过在输入空间直接检索并采用拼接式聚合，替代嵌入空间检索与平均聚合，保留时间结构并降低推理开销。在多个基准数据集上，RAEF相比RAF在精度与效率上均占优，且能达到与微调相当或更优的性能。这使其成为时间序列领域适应的一种实用、可扩展的轻量方案。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2608.14054v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 582, \"height\": 433, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2608.14054v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 892, \"height\": 751, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2608.14054v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 828, \"height\": 126, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2608.14054v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 558, \"height\": 276, \"label\": \"Table\"}]"
motivation: 微调成本高，现有RAF在嵌入空间检索和平均聚合有局限，需更高效的模型无关检索增强方案。
method: RAEF基于RAF，在输入空间直接检索近邻序列，用拼接而非平均聚合保留时序结构，降低开销并提升精度。
result: 多个基准上RAEF优于RAF，与微调性能相当或更好，推理开销更低。
conclusion: RAEF是低成本、可扩展的领域适配方案，适合数据稀缺的时间序列预测。
---

## 摘要
基于预训练基础模型的时间序列预测已展现出强大的零样本能力。然而，在特定领域应用中，对于历史数据较短或可忽略的时间序列，要实现最优性能通常需要通过微调或RAG进行适配。虽然微调有效，但会产生大量计算成本。本研究在单变量时间序列中探索RAG（检索增强生成）作为一种更高效的替代方案，特别是RAF（检索增强预测），并引入了RAEF（检索增强扩展预测），一种基于RAF构建的模型无关方法。RAEF对检索和聚合机制进行了关键改进：(1) 在输入空间而非嵌入空间进行直接检索，减少推理开销；(2) 采用基于拼接的聚合而非平均，以保留时间结构。跨多个基准数据集的实证评估表明，RAEF在准确性和推理开销方面均优于RAF。此外，与零样本和微调基础模型的全面比较显示，RAEF在避免计算负担的同时，实现了与微调相当或更优的性能，使其成为时间序列预测中领域适配的一种实用且可扩展的方法。

## Abstract
Time series forecasting with pretrained foundation models has demonstrated strong zero-shot capabilities. However, achieving optimal performance on time series with short or negligible historical data in domain-specific applications typically requires adaptation via either fine-tuning or RAG. While fine-tuning is effective, it incurs substantial computational costs. This work explores RAG within univariate time series (Retrieval Augmented Generation) as a more efficient alternative, in particular RAF (Retrieval Augmented Forecasting), and introduces RAEF (Retrieval-Augmented Extended Forecasting), a model-agnostic method built upon RAF. RAEF incorporates key refinements to the retrieval and aggregation mechanisms: (1) direct retrieval in input-space rather than embedding-space, reducing inference overhead, and (2) concatenation-based aggregation that preserves temporal structure instead of averaging. Empirical evaluation across multiple benchmark datasets demonstrates that RAEF outperforms RAF in both accuracy and inference overhead. Furthermore, comprehensive comparisons with zero-shot and fine-tuned foundation models show that RAEF achieves competitive or superior performance to fine-tuning while avoiding its computational burden, establishing it as a practical and scalable approach for domain adaptation in time series forecasting.

---

## 论文详细总结（自动生成）

## 1. 论文核心问题与整体含义（研究动机和背景）

- **研究背景**：基于 Transformer 的时间序列基础模型（如 Moirai、Chronos、TimesFM 等）在零样本预测中已展现出色能力。但在**历史数据短缺的场景**下，零样本方法难以达到满意的精度，通常需要领域适配。
- **现有问题**：
  - **微调**虽有效，但计算成本高，且与模型规模和数据集规模成正比；
  - **已有的时间序列 RAG 方法**（如 TimeRAF、RATD、RAFT、TS-RAG）大多需要定制架构或领域特定训练，模型无关性有限；
  - **RAF** 是少数保持模型无关的方法，但其嵌入空间检索引入了额外计算开销，且其“未来取平均”的聚合策略会稀释重要时间模式，限制精度提升。
- **核心问题**：是否存在一种**无需参数更新**、**计算高效**、**模型无关**的方案，既能优于现有 RAG 方法，又能逼近或超越微调的效果？

## 2. 论文提出的方法论

**核心思想**：在 RAF 框架基础上做两项关键改进——**在输入空间直接检索**（替代嵌入空间检索）和**基于拼接的聚合**（替代平均聚合），从而提出 RAEF（Retrieval-Augmented Extended Forecasting）。

### 2.1 检索器（Retriever）
- 沿用 RAF 的检索机制，但**省略嵌入步骤**；
- 理由：时间序列本身是实值连续向量，欧氏距离等度量在输入空间中已可直接衡量相似性，额外嵌入只会增加开销，且可能造成信息损失。

### 2.2 聚合器（Aggregator）
- 对输入 x 做实例归一化，得到  x̃ 及均值 μx、标准差 σx；
- 对检索样本的分数 s 进行归一化：

  x̃r = (xr − μx) / σx ,　s̃ = s / (σx²·C)

- 基于归一化距离阈值 dt 将检索样本分为两类：

  - **候选集 C**（高相关，s̃ ≤ dt）：直接**拼接**为 rcan，保留完整时序结构；
  - **残差集 R**（低相关，s̃ > dt）：取**平均**得到 rres，不完全丢弃但压缩信息量。

- 最终增强输入：

  xaug = concat(rres, rcan, x̃)

- 同时**移除 RAF 中的对齐（alignment）步骤**：模型会将检索样本视为一种“周期性历史表征”，自然地去捕捉扩展的时序模式。

### 2.3 关键设计逻辑
- 拼接保留单个样本的时间结构，让注意力机制能有针对性地关注各检索样本中的不同模式；
- 阈值 dt 在“过长的拼接序列导致计算负担”和“过度平均导致信息丢失”之间取得平衡；
- 实验表明 dt = 1.0 效果最佳，对应一个标准差内的样本具有高分布相似性。

## 3. 实验设计

- **数据集**：6 个基准数据集，覆盖能源（ET）、云资源（Huawei Cloud）、电力（Power、ElectricityUCI）、流量（Traffic）、宏观经济（FredMd）等多样化领域。
- **基础模型**：
  - 主线：MoiraiMoE（推理时间 16ms）；
  - 次要：Chronos-T5、Chronos-Bolt，用于验证模型无关性。
- **基准设置**：
  - 预测视界 F = 16（固定）；
  - 输入上下文长度 C ∈ {32, 64, 128}（额外探索了 C = 256、512）；
  - 向量数据库包含 10,000 条训练样本，使用 HNSW + Chroma 框架构建，构建时间约 5 分钟；
  - k = 16 个检索样本；
  - 3 个随机种子，MASE 标准差控制在 0.001–0.01。
- **对比方法**：
  1. **Base**：无适配的预训练模型；
  2. **Fine-Tuning**：全参数微调（lr=1e-4，batch=1024，weight decay=1e-2，early stopping 容忍度 3，最大 500 迭代）；
  3. **RAF**：嵌入空间检索 + 对齐 + 平均聚合；
  4. **RAEF**：本文方法。
- **评估指标**：MASE（Mean Absolute Scaled Error）。

## 4. 资源与算力

- **论文未明确说明 GPU 型号、数量或训练总时长**；
- 但提供了一些相关的计算效率数据：
  - 向量数据库构建约 5 分钟；
  - RAEF 检索开销约 **2ms/查询**，RAF 约为 **8ms/查询**（降低约 4 倍）；
  - 微调以 500 次迭代为上限，配合 early stopping（patience=3）；
  - 所有实验用 3 个随机种子，说明总实验量是有一定规模的，但具体算力未披露。

## 5. 实验数量与充分性

- **实验规模**：
  - 主表（Table 1）覆盖 3 个基础模型 × 6 个数据集 × 3 种上下文长度 × 4 种方法，规模较大；
  - 消融实验（表 3）涵盖移除聚合器、嵌入空间 + L2/余弦距离等变体；
  - 超参数分析（表 2）对 dt 在 {0.1, 0.5, 1.0, 1.5, 2.0, 3.0} 范围内进行验证。
- **积极作用**：
  - 首次在时间序列 RAG 领域系统对比 RAG 与微调，定位明确且具有贡献性；
  - 在 Chronos 模型上**排除了预训练数据集中出现过的数据集**（标 ×），避免数据泄漏，严谨性较好。
- **充分性方面的保留意见**：
  - 未与其他近年 RAG 时间序列方法（TimeRAF、RATD、RAFT、TS-RAG）做直接数值对比（仅引用说明其非模型无关）；
  - 实验集中在**单变量**时间序列，未扩展到多变量场景；
  - 在部分数据集（Chronos-Bolt 的 Context 64 等）上 RAEF 出现负改进（−1.48%），论文仅用“基础性能强”解释，未有深入分析；
  - 对于“C ≥ 256 后收益下降”的结论，只给出趋势描述，缺少充分数据支撑。

## 6. 论文的主要结论与发现

- RAEF 在 MoiraiMoE 上相对基线实现 **11–16% 的 MASE 改善**，一致优于 RAF（1–7%）；
- 在 4/6 数据集上，RAEF 达到与微调相当或更优的性能，且**无需任何参数更新**；
- **上下文越短，RAG 收益越大**：C=32 时改善 15.80%，C=128 时降至 11.26%，C=256 时约 6%，C=512 时接近 0%；
- 输入空间检索相比嵌入空间检索将检索耗时降低约 4 倍，且不影响检索质量；
- 拼接式聚合优于平均式聚合，注意力机制能够有效利用各检索样本中的独立时序模式；
- 模型无关性得到验证：RAEF 在三个不同架构的基础模型上均带来一致性提升。

## 7. 优点

- **模型无关设计**：无需修改任何基础模型参数，即插即用，适用于各种预训练模型；
- **高效且轻量**：输入空间检索省去嵌入步骤，推理开销显著降低，工程实现简单；
- **方法直观且可解释**：拼接高相似样本、平均低相似残差的思路清晰合理；
- **首次对比 RAG 与微调**：直接回答了“RAG 在时间序列中能否替代微调”这一关键问题，且给出了积极回答；
- **识别的核心规律有启发意义**：RAG 的收益随上下文长度递减，对实际场景中何时使用 RAG 具有指导意义；
- **数据泄漏防护意识强**：在 Chronos 模型实验中明确排除了预训练时代出现过的数据集。

## 8. 不足与局限

- **实验范围有限**：仅覆盖单变量时序，未处理多变量场景；数据集虽跨领域，但每个领域仅 1–2 个数据集；
- **未与其他 RAG 方法直接数值比较**：论文只与 RAF 做了对比，无法判定 RAEF 是否优于所有现有时间序列 RAG 方法；
- **潜在数据泄漏风险**：MoiraiMoE 的预训练数据集合未公开，Power、Traffic 等数据集可能已参与其预训练；
- **超参数依赖**：阈值 dt 的选取依赖经验设定，缺乏自适应机制；k、数据库容量等也未做敏感性分析；
- **阈值有效性解释较简略**：仅在 C=32（MoiraiMoE）上给出阈值分析，未覆盖其他上下文长度、数据集或模型；
- **部分负面结果未深入讨论**：Chronos-Bolt 在部分设置下出现性能退化，论文的解释较为表面；
- **微调对比的公平性有待商榷**：微调上限仅 500 迭代，且早停止 patience 设 3，这对“微调”是一种较弱的基准配置；若增加迭代次数或采用更充分的正则化，微调效果可能更强；
- **理论基础薄弱**：为何在输入空间直接检索即可替代嵌入空间检索，缺乏理论分析与保障；仅依赖经验证据；
- **算力资源未报告**：无法评估其整体训练/推理成本在绝对意义上的水平。

（完）
