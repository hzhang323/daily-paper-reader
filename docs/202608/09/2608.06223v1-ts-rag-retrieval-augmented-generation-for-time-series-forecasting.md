---
title: "TS-RAG: Retrieval Augmented Generation for Time Series Forecasting"
title_zh: TS-RAG：用于时间序列预测的检索增强生成
authors: "Yixiong Xiao, Congxi Xiao, Jingbo Zhou"
date: 2026-08-06
pdf: "https://arxiv.org/pdf/2608.06223v1"
tags: ["query:time-series"]
score: 9.0
evidence: 将检索增强生成与大语言模型应用于时间序列预测，直接相关
tldr: 时间序列预测中，基于Transformer的模型虽表现优异，但检索增强生成（RAG）的应用尚属空白。现有方法受限于训练数据少、参数规模小，难以像大语言模型那样直接拼接参考序列。为此，本文提出TS-RAG框架，设计专门参考标记（reference tokens）以融合输入序列与检索到的相似序列信息，更稳健地捕捉复杂时间动态。在多个真实世界基准上，TS-RAG取得一致的最优性能，验证了RAG在时间序列预测中的有效性。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2608.06223v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1820, \"height\": 648, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2608.06223v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1822, \"height\": 1112, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2608.06223v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1818, \"height\": 713, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2608.06223v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1465, \"height\": 1215, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2608.06223v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1831, \"height\": 313, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2608.06223v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 923, \"height\": 254, \"label\": \"Table\"}]"
motivation: 时间序列模型训练数据有限、参数规模小，直接套用大语言模型的RAG拼接方式效果不佳，急需适配的检索增强方法。
method: TS-RAG引入专门设计的参考标记（reference tokens），将输入序列与检索到的相似序列信息有效融合，强化时序动态建模。
result: 在多个真实世界预测基准上，TS-RAG均取得一致的最先进（SOTA）性能，显著优于现有方法。
conclusion: TS-RAG首次将RAG成功应用于时间序列预测，通过参考标记融合检索信息，为提升预测精度提供了新范式。
---

## 摘要
尽管深度学习模型，特别是基于Transformer的架构，在时间序列预测中表现出色，但检索增强生成（RAG）在该领域的应用仍然有限。鉴于RAG通过整合相关外部信息在增强大型语言模型能力方面已被证明有效，检索相似的时间序列序列作为参考也可能提高时间序列预测任务的准确性。然而，大多数时间序列模型受到训练数据有限、参数规模较小以及缺乏大型语言模型所具备的广泛生成能力的限制。像语言模型中那样简单地将参考序列拼接到提示中，可能无法产生预期的结果。为了解决这些挑战，我们提出了一种新颖的方法TS-RAG，利用RAG来提升预测性能。该框架引入了专门设计的参考令牌，以有效融合输入序列和检索到的相似序列的信息，从而更稳健地捕捉复杂的时间动态。实验结果表明，TS-RAG在多个真实世界的预测基准上持续实现了最先进的性能。

## Abstract
While deep learning models, particularly transformer-based architectures, have shown impressive performance in time series forecasting, the application of retrieval-augmented generation (RAG) in this domain remains limited. Since RAG has proven effective in enhancing the capabilities of large language models by incorporating relevant external information, retrieving similar time series sequences as references might also improve accuracy in time series forecasting tasks. However, most time series models are constrained by limited training data, smaller parameter scales, and a lack of the extensive generative capabilities found in large language models. Simply concatenating reference sequences into the prompt, as done in language models, may not yield the expected results. To address these challenges, we propose a novel approach, TS-RAG, which leverages RAG to enhance forecasting performance. The framework introduces specially designed reference tokens to effectively fuse information from the input sequence with that from retrieved similar sequences, enabling a more robust capture of complex temporal dynamics. Experimental results demonstrate that TS-RAG achieves consistent state-of-the-art performance across several real-world forecasting benchmarks.

---

## 论文详细总结（自动生成）

## TS-RAG 论文详细总结

### 1. 论文的核心问题与整体含义（研究动机和背景）

- **研究背景**：基于 Transformer 的深度模型已在时间序列预测中取得显著成功，但绝大多数现有工作仍聚焦于改进模型训练策略（如多尺度混合、时间成像等），仅依赖输入序列自身提取特征，忽视了对外部相似历史序列的利用。
- **核心问题**：检索增强生成（RAG）在 NLP 领域已被证明能有效提升大语言模型的知识利用能力，但在时间序列预测领域的应用十分稀少。直接照搬 NLP 中"将检索序列拼接进输入"的做法在时序模型上效果不佳，因为时间序列模型普遍存在以下三方面局限：
  - 训练数据规模有限；
  - 模型参数量较小；
  - 缺乏 LLM 所具有的强泛化与生成能力。
- **现有检索方法的效率问题**：传统的相似序列检索方法（如 DTW）计算开销极大，严重影响推理效率，限制了实际应用场景。
- **论文意义**：本文试图弥合 RAG 与时间序列预测之间的鸿沟，探索如何让 RAG 以结构化、有意义的方式为时序预测提供外部参考信息。

### 2. 论文提出的方法论：核心思想、关键技术细节

**（1）整体框架**
- 提出 **TS-RAG** 框架，利用检索增强机制提升时间序列预测精度。
- 框架由三个关键部分构成：**嵌入（Embedding）**、**检索（Retrieval）** 与 **参考融合（Reference Fusion）**。

**（2）检索模块**
- 从历史数据库 D 中基于相似度检索函数 R，选出与当前输入最相似的 N 条历史序列，公式如下：
  - $r^{(i)} = \mathcal{R}(x^{(i)}, D) = \{ r^{(i,n)} \}_{n=1}^{N}$
- 检索采用 **TCN（时间卷积网络）** 训练得到，相比 DTW 大幅提升检索效率，也可用欧氏距离等替代方案进行消融对比。

**（3）输入序列嵌入（参考令牌设计）**
- 对输入序列各通道进行 **非重叠 patch 化**（patch 长度为 p），得到 patch 化表示。
- 核心创新：为每条检索到的参考序列引入**可学习的参考令牌（reference tokens）** $v_{ref}^{(n)}(c)$，作为历史模式"压缩表示"。
- 这些参考令牌被拼接在 patch 化输入序列之前，形成增强后的输入表示：
  - $\tilde{x}_{patch}^{(i,c)} = [v_{ref}^{(1)}(c), v_{ref}^{(2)}(c), ..., v_{ref}^{(N)}(c), x_{patch}^{(i,c)}]$
- 最终通过线性变换与位置编码得到输入嵌入：
  - $x_{embed}^{(i)} = W_x \tilde{x}_{patch}^{(i)} + b_x + PE(\tilde{x}_{patch}^{(i)})$

**（4）参考序列嵌入**
- 每条检索到的参考序列独立进行 patch 化，使用与输入序列相同的线性变换及位置编码生成嵌入。

**（5）参考融合机制**
- 采用两种注意力机制实现信息整合：
  - **自注意力（Self-Attention）**：捕捉输入序列内部的时间依赖关系；
  - **交叉注意力（Cross-Attention）**：以输入序列嵌入为 Query，参考序列嵌入为 Key/Value，从而将检索到的历史模式有选择地融入当前输入特征，实现跨序列信息的对齐与萃取。
- 最终隐藏表示经前馈网络后输出预测结果 $\hat{x}_{L+1:L+T}^{(i)}$。

**（6）模型变体**
- **TS-RAG**：不显式建模通道间依赖；
- **TS-RAG-CM**：额外考虑通道间依赖（Channel Mixing），性能更强。

### 3. 实验设计：数据集、基准与对比方法

**（1）数据集（6 个公开基准）**
- **ETTh1 / ETTh2**：小时级电力变压器温度/负载数据；
- **ETTm1 / ETTm2**：分钟级电力变压器数据；
- **Electricity（ECL）**：321 个用户的电力消费数据；
- **Weather**：21 个气象指标（温度、湿度、风速、气压等）。
- 统一设置：输入序列长度 L=96，预测长度 S ∈ {96, 192, 336, 720}。数据划分遵循 TimeXer、iTransformer 的标准预处理协议。

**（2）对比方法**
- **Transformer 类基线**：Informer、Autoformer、FedFormer、PatchTST、Crossformer、TimesNet、iTransformer、TimeXer；
- **MLP/线性类基线**：DLinear、RLinear、TiDE；
- 超参数设置与 iTransformer 保持一致，保证公平对比。

**（3）评价指标**
- 均方误差（MSE）和平均绝对误差（MAE），所有结果取 5 次独立运行的平均值。

### 4. 资源与算力

- **硬件**：NVIDIA V100 GPU，32GB 显存（论文明确说明）。
- **运行框架**：PyTorch。
- **未明确说明的信息**：
  - GPU 数量（1 张或多张）未明确；
  - 总训练时长及各数据集训练时间未报告；
  - 模型参数量、FLOPs 等计算成本指标未给出。
- **检索效率数据**（消融实验中有明确报告）：
  - TCN 检索：0.004 秒
  - 欧氏距离（ED）检索：0.002 秒
  - DTW 检索：14.27 秒（比 TCN 慢约 3500 倍）
- **训练细节**：Adam 优化器（β1=0.9, β2=0.999），学习率 0.001，余弦退火调度。

### 5. 实验数量与充分性分析

**（1）实验总量**
- 主实验：6 个数据集 × 4 种预测长度，涵盖 10+ 个基线对比模型；
- 消融实验共 3 组：
  - ① 与"直接拼接参考序列"方法的对比；
  - ② 参考序列数量（1/2/4）的影响分析；
  - ③ 不同检索方法（TCN / DTW / ED）的性能与效率对比。

**（2）充分性与客观性评价**
- **优点**：实验覆盖多个领域的真实数据集，与主流 SOTA 模型进行了系统对比；消融实验设计针对性强，分别验证了融合策略、检索数量、检索方法三个关键因素的有效性；5 次重复实验取均值降低了随机误差。
- **不足**：
  - 论文未提供置信区间或显著性检验，无法判断差异是否统计显著；
  - 未报告每个数据集在每种预测长度下的详细结果（Table 1 只展示了四种预测长度的平均值，单长度结果被省略）；
  - 对比基线中未包括正文提及的近期检索增强方法如 RAFT，也未包括时间序列基础模型（如 TimeGPT-1、TimesFM、MOIRAI 等）。考虑到这些都是相关领域的重要对比对象，会使说服力受限；
  - 消融实验是否在所有数据集上进行未明确说明（图 3、4 以及表 2 的展示似乎集中在 ETTm2 上），结论的普适性有待验证。

### 6. 论文的主要结论与发现

- **TS-RAG-CM 在全部 6 个数据集上均取得最优或接近最优的结果**，平均 MSE（0.310）与 MAE（0.348）刷新所有基线，验证了 RAG 在时间序列预测领域的有效性。
- **直接拼接参考序列在时序模型中效果不佳**：相比 TS-RAG 的参考令牌融合方案，直接拼接方式的 MSE 在不同预测长度下高出 12.1%–18.2%，说明时间序列模型缺乏 LLM 的强大生成能力，需要专门设计的融合机制。
- **单一参考序列最优**：检索 1 条序列时效果最好，增加至 2 条或 4 条反而导致性能下降，说明多余参考可能引入冗余或矛盾的模式。
- **TCN 检索在精度与效率间取得最佳平衡**：远超 DTW 的效率（快约 3500 倍），且精度略优于欧氏距离和 DTW。
- TS-RAG（不建模通道依赖）的表现已能与显式引入通道混合的 iTransformer 相当，进一步凸显了 RAG 机制本身的贡献。

### 7. 优点：方法与实验设计上的亮点

- **领域创新性**：较早系统性地将 RAG 思想引入时间序列预测领域，探索了 NLP 技术跨域迁移的可行性；
- **参考令牌设计**：借鉴多模态"全局令牌"思路，以可学习参考令牌压缩检索序列信息，避免简单拼接带来的信息冗余和融合困难；
- **融合机制清晰**：自注意力与交叉注意力的组合使得模型既能保留输入序列自身的时序依赖，又能选择性提取参考序列中的有用模式；
- **效率意识强**：明确指出 DTW 的计算瓶颈并提出 TCN 向量检索替代方案，兼具精度与实时性；
- **消融设计针对性强**：三组消融实验分别回答"是否需要特殊融合""需要多少参考序列""用什么检索"三个核心问题，逻辑链条完整。

### 8. 不足与局限

- **作为预印本**，论文未经同行评审，结论的可靠性有待进一步验证；
- **实验细节不完整**：缺少各预测长度下的分项结果、置信区间、训练开销统计、模型参数量等关键信息；
- **基线覆盖不全**：未与同为检索增强方法的 RAFT 做直接对比，也未评估与时间序列基础模型（TimeGPT-1、TimesFM、MOIRAI 等）之间的相对表现，削弱了对方法定位的说明；
- **通用性证据有限**：实验集中在能源和气象数据集，未涉及金融、健康、交通等时间序列典型场景；消融实验在不同数据集上的表现需要更充分的验证；
- **检索机制解释性不足**：论文未深入分析检索到的参考序列如何具体影响预测结果，参考令牌的可解释性不明；
- **未来方向未充分讨论**：检索数据库如何随新数据动态更新、长序列输入的扩展性、跨数据集检索等现实问题未展开讨论。

（完）
