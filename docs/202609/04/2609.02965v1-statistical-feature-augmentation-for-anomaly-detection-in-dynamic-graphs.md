---
title: Statistical Feature Augmentation for Anomaly Detection in Dynamic Graphs
title_zh: 动态图中异常检测的统计特征增强
authors: "Philipp Schlinge, Jean-Luc Schnipper, Martin Atzmueller"
date: 2026-09-02
pdf: "https://arxiv.org/pdf/2609.02965v1"
tags: ["query:time-series"]
score: 9.0
evidence: 在动态图的时间事件流上进行异常检测，通过统计特征增强捕捉短期行为交互异常。
tldr: 动态图异常检测需兼顾时序结构与特征信息，但现有深度模型难以直接从原始事件流中学习短期行为交互信号。提出统计特征增强方法，将行为交互统计量显式编码进输入空间。在三个数据集和七种模型上验证，该方法持续提升检测性能。同时支持细粒度行为重要性分析，展示了传统网络分析与深度学习融合的潜力。
source: arxiv
selection_source: fresh_fetch
tables_json: "[{\"url\": \"assets/tables/arxiv/2609.02965v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 882, \"height\": 314, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2609.02965v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 859, \"height\": 425, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2609.02965v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1331, \"height\": 965, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2609.02965v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1150, \"height\": 1006, \"label\": \"Table\"}]"
motivation: 深度模型难以从原始事件流直接捕捉发送强度、交互惯性等短期行为信号。
method: 提出统计特征增强方法，将行为交互统计量显式编码到输入特征空间。
result: 在Reddit、Wikipedia、MOOC三个数据集上，该方法对七种连续时间与离散时间模型均持续提升异常检测性能。
conclusion: 统计增强的特征不仅提升性能，还支持细粒度行为重要性分析，为传统网络分析与深度学习的融合提供了新思路。
---

## 摘要
动态网络正被应用于从社交媒体到物流系统的众多领域，每个领域都有其自身的特殊特征。用于此类数据的模型必须捕捉时间/结构与基于特征的信息之间的二元性。然而，最先进的深度学习模型往往难以直接从原始事件流中学习特别是短期行为交互信号，如发送者强度或交互惯性。为弥补这一差距，我们提出了一种统计特征增强方法，将行为交互统计显式编码到输入特征空间中。我们在三个真实世界数据集（Reddit、Wikipedia、MOOC）和七种涵盖连续时间和离散时间架构的模型上评估了所提出的方法在异常检测任务上的表现。作为基线，我们应用了在原始嵌入上训练的相同模型。我们的结果表明，增强始终能提高检测性能。在性能之外，增强后的输入能够对行为重要性进行细粒度的事后分析，因为每个统计量都占据一个专用的输入维度。特别是，这项工作展示了一种将经典网络分析与深度学习相结合的有前景的方法。

## Abstract
Dynamic networks are being applied in many domains, from social media to logistics systems, each with their own set of special characteristics. A model employed on this type of data must capture the duality between temporal/structural and feature-based information. Yet state-of-the-art deep learning models often struggle to learn especially short-term behavioral interaction signals, such as sender intensity or interaction inertia, directly from raw event streams. To address this gap, we propose a statistical feature augmentation method that explicitly encodes behavioral interaction statistics into the input feature space. We evaluate our proposed method on an anomaly detection task across three real-world datasets (Reddit, Wikipedia, MOOC) and seven models spanning both continuous-time and discrete-time architectures. As a baseline, we apply the same models trained on the original embeddings. Our results show, that augmentation consistently improves detection performance. Beyond performance, the enriched input enables fine-grained post-hoc analysis of behavioral importance, since each statistic occupies a dedicated input dimension. In particular, this work showcases a promising approach for merging classical network analysis with deep learning.

---

## 论文详细总结（自动生成）

## 1. 论文核心问题与整体含义

- **研究动机**：动态图（如社交网络、MOOC论坛、百科编辑网络）中的异常检测需要同时捕捉时间/结构与节点/边特征信息，但现有深度学习方法（无论是连续时间还是离散时间架构）难以直接从原始事件流中学习**短期行为交互信号**，例如发送者在一段时间内的活动强度（intensity）、特定边上的交互惯性（inertia）以及循环交互模式（cyclic patterns）。
- **核心思路**：论文认为，传统的网络分析（如经典的社会网络统计量）能够显式描述这些行为特征，但尚未被深度模型有效地利用。因此，作者提出一种**统计特征增强**方法，将行为交互统计量显式编码为输入特征，使任意深度架构都能直接访问这些信息，并借助每个统计量占有一个专用输入维度来实现细粒度的可解释性分析。
- **整体含义**：这项工作探索了经典网络分析与深度学习融合的可行路径，既提升了异常检测性能，也增强了模型决策的透明度，属于“数据层面/预处理”而非“架构层面”的改进。

## 2. 论文提出的方法论

- **核心思想**：将事件流中被隐式编码的短期行为统计量提取出来，附加到每个事件的特征向量上，使模型不必再“自行学习”这些统计规律。
- **事件流定义**：事件序列 \(E=\{e_t\}\)，每个事件 \(e_t=(s_t, r_t, t, x_t)\)，包含发送者、接收者、时间戳和原始边特征 \(x_t\)。
- **时间衰减权重**：对所有历史事件赋予指数衰减权重：
  \[
  w(u,t)=\lambda e^{-\lambda(t-u)},\quad \lambda=\ln2/T_{1/2}
  \]
  其中 \(T_{1/2}\) 是半衰期，控制历史事件的影响范围；实验中将半衰期设置为数据集时长的一半。
- **使用的统计量**（见表1）：
  - **节点强度（Node Intensity）**：发送者/接收者的整体发生活动量；
  - **边惯性（Edge Inertia）**：特定发送者-接收者边上的近期活动量；
  - **二元交互循环（Pair interaction cycle, C2）**：发送者是否在连续改变接收者后回到同一个对象；
  - **三元交互循环（Triad interaction cycle, C3）**：类似C2但涉及三个不同接收者的循环模式。
  - 统计量还区分有向/无向交互，最终每个事件得到**9个统计量**。
- **增强变体1（+Stats）**：将归一化的统计向量直接拼接到原始特征上：
  \[
  x'_t = [x_t; s(t)]
  \]
- **增强变体2（+Stats+Encoded）**：为了避免特征空间分布不对称，用一个带瓶颈的自编码器对拼接后的向量再做投影。其训练损失包括：
  - **KS分离项**——使正常与异常样本在潜在维度上的经验分布距离最大化；
  - **重构误差**——保留输入信息；
  - **方差惩罚**——防止潜在维度塌缩。
- **计算效率**：由于采用指数衰减权重，每个统计量可在常数时间内更新，整个事件流的处理复杂度为 \(O(n)\)。

## 3. 实验设计

- **任务**：动态图上的二元边级异常检测：给定历史事件流，判断当前事件是否异常。
- **数据集**：
  - **Reddit**（约47万训练事件，异常率0.04%）
  - **Wikipedia**（约11万训练事件，异常率0.14%）
  - **MOOC**（约28.8万训练事件，异常率1.03%）
  - 三个数据集均带真实异常标签，按时间顺序70/15/15划分训练/验证/测试。
- **对比模型**：
  - 连续时间模型：TGAT、TGN、DyGFormer、AER、SAD；
  - 离散时间模型：GraphSAGE、StrGNN。
- **三种输入配置**：
  1. 原始嵌入（Baseline）
  2. 原始嵌入+统计量拼接（+Stats）
  3. KS自编码器编码后表示（+Stats+Encoded）
- **评价指标**：ROC-AUC；另用SHAP衡量统计特征与原始嵌入对预测的重要性占比。
- **实现细节**：模型采用各原仓库推荐默认参数；离散时间模型先按2000条边快照化；所有实验在4个随机种子下重复并报告均值±标准差。

## 4. 资源与算力

- 论文正文**没有明确报告**GPU型号、数量、训练时长或总计算开销，也未提及能源/碳等资源信息。
- 仅在方法部分指出统计量计算具有线性复杂度 \(O(n)\)，说明其预处理开销相对较低，但整体实验的硬件配置和耗时均未披露。

## 5. 实验数量与充分性

- **主实验规模**：3个数据集 × 7个模型 × 3种输入配置 = **63组实验结果**，每组结果重复4个随机种子，涵盖了主流连续时间与离散时间架构。
- **附加分析**：对所有+Stats和+Stats+Encoded配置进行了SHAP重要性分解，并与性能最高配对交叉引用。
- **充分性评价**：
  - **优点**：数据集多样性较好，模型覆盖广，重复随机种子提供了统计稳定性；
  - **不足**：缺乏消融实验——例如不同统计量子集的贡献、半衰期/时间窗口敏感性、对不同异常密度的影响等；也没有与基于统计建模（如REM/STREAM/DREAM）或经典异常检测方法进行显式比较；没有给出统计量增强在欠采样/类别不平衡下的鲁棒性分析。总体实验设计扎实，但仍有可扩展空间。

## 6. 论文的主要结论与发现

- **性能提升**：在大多数配置下，加入统计特征能提高ROC-AUC，且部分模型提升显著（例如Reddit上GraphSAGE提升+21.18%，MOOC上TGAT提升+11.98%）。
- **模型依赖性**：没有某一特定架构在所有数据集上一致获益最多；增强效果依赖于数据集的特征，而不仅取决于模型家族。
- **编码变体的作用**：+Stats+Encoded通常优于简单拼接，能进一步增大统计特征的SHAP重要性并降低随机种子间的方差，但该效应并非在所有数据集和模型上都一致。
- **可解释性**：统计量仅占9个输入维度，但往往承担了很大比例（甚至80%以上）的决策重要性，说明其携带的信息对异常检测有独特价值；这些特征也能被SHAP直接利用，使行为层面的事后解释成为可能。

## 7. 优点

- **模型无关与普适性**：增强发生在输入层面，不限定网络架构，可无缝应用于连续时间与离散时间模型。
- **计算高效**：线性时间预处理可扩展到大流数据集。
- **显著提升与稳定性**：对低性能和高性能模型都有增益，并非单纯“补短板”；增加统计量还通常减少种子间的性能波动。
- **改善可解释性**：每个统计量对应专用输入维度，可直接用SHAP等进行事后分析，解决了动态图模型中行为解释难的问题。
- **融合经典与深度方法**：将社会网络分析中的经典统计量重新引入深度学习流程，展示了传统知识增强现代模型的有效性。

## 8. 不足与局限

- **统计量种类有限**：只使用了强度、惯性和二/三元循环共9个统计量，未覆盖更复杂的模式（如角色变化、路径/社区模式等）。
- **编码设计的潜在偏差**：+Stats+Encoded中的KS损失依赖训练集标签，将异常检测信息提前介入预处理，这可能削弱“纯无监督/半监督”假设；且对高度类别不平衡的鲁棒性未深入分析。
- **结果方差仍然较高**：多处报告的标准差较大（尤其DyGFormer在MOOC上的方差随编码而增大），说明稳定性提升并非普遍规律。
- **超参固定**：\(T_{1/2}\)统一取数据集时长的一半，没有验证不同半衰期的影响；缺少关键超参数的敏感性分析。
- **比较基准不够全面**：未与REM/STREAM/DREAM等专门建模统计效应的模型，或经典方法（如基于规则/统计检验）比较；也未和基于随机特征/重构误差的通用增强方法对比。
- **资源信息缺失**：未报告训练算力、时间成本，可复现性在同等硬件条件下仍需要额外实验摸索。

（完）
